var util = require('util')
  , EventEmitter = require('events').EventEmitter

var EventContainer = function (otions) {

}

util.inherits(EventContainer, EventEmitter)
module.exports = EventContainer

